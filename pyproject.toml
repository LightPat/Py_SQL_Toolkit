[project]
name = "py-sql-toolkit"
version = "0.2.1"
description = "A toolkit for modifying and building SQL queries directly in Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sqlglot>=30.9.0",
]

[dependency-groups]
dev = [
    "bumpversion>=0.6.0",
]
